# Vome 插件规范（全栈 · vome-plugin-full）

> 目录：`plugins/vome-plugin-full` · key：`scaffold-full`  
> **用法 / Agent**：见 [`.vscode/skills/SKILL.md`](./.vscode/skills/SKILL.md)

## 可用 / 不可用

| 能力 | 状态 |
|------|------|
| 后端 `BasePlugin` / invoke / `handlers`→`/admin/ext/{key}` | **可用** |
| 前端 Vue、Hash 路由、Pinia、主题 / 语种同步 | **可用** |
| `hostRequest` / `extPath`（无感 refresh） | **可用** |
| 可选 EPS：`bootHostEps` + `service.*` | **可用** |
| 插件内完整宿主 CRUD / IoC | **不可用** |
| 前端 `base: '/'` | **禁止**（须 `base: './'`） |
| 插件内切语言 UI | **禁止**（跟随 Admin） |

## 目录与清单

| 路径 / 字段 | 约定 |
|-------------|------|
| `src/` | 后端 → 打包 `server/` |
| `web-src/` | 前端源码 → 打包 `web/` |
| `locales/` | 源语言包（包根；勿只放 `web-src/`） |
| `PLUGIN_KEY` / `module.json.key` / `menus[].appKey` | **三者一致**；微应用菜单挂 **「无界渲染」**，页面无 icon |
| `routes` | 与 `handlers` 一一对应 |

## 后端（强制）

- `export const Plugin` + `extends BasePlugin`
- 混淆须保留公开方法名 / handler 名（否则 `invoke` 失效）
- **可用**宿主注入的 core 能力：`BasePlugin`、config、cache、`pluginService`
- **禁止**把整包 `vome-core/server` 打进 `.vome`（`vome-plugin-runtime` 须 `--external`）

## 前端 Vue（强制）

| 项 | 约定 |
|----|------|
| 源码根 | `web-src/`（`@` → `web-src`） |
| 路由 | **仅** Hash；`pages/**/*.vue` 自动路由 |
| 入口壳 | `App.vue` 须挂主题 / 语种同步 |

## 主题 / 语种（强制）

| 项 | 约定 |
|----|------|
| 根节点 | 透明底；禁止再铺全页白底 |
| 样式 | 宿主 CSS 变量 |
| 主题 / 语种 | bus `vome-host-theme` / `vome-host-locale` |
| 文案 | `t('a.b')`；译包权威宿主 `i18n_pack` |

## 产物

`pack` = 混淆后端 + `build:web` + `locales/` → `.vome`（`server/` + `web/`）。勿含 `src` / `node_modules`。

## 交付自检

- [ ] `bun run pack`，前后端可安装验证
- [ ] ext + 侧栏微应用通；主题 / 语种跟宿主
- [ ] 无插件内切语言 UI；三者 key 一致
