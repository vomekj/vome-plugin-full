# Vome 插件规范（全栈 · vome-plugin-full）

> 目录：`plugins/vome-plugin-full` · key：`scaffold-full`

## 可用 / 不可用

- **可用**：后端 invoke/ext + 前端 hostRequest/extPath + menus/wujie  
- **不可用**：插件内完整宿主 CRUD/IoC  

## 目录

| 路径 | 用途 |
|------|------|
| `src/` | 后端 → `server/` |
| `web-src/` | 前端 → `web/`（含 `lib/host-api.ts`） |

## 产物

`pack` = 混淆后端 + `build:web`；`PLUGIN_KEY` 与 `module.json.key` 一致。
