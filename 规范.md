# Vome 插件规范（前端 + 后端脚手架）

## 清单 `module.json`

| 字段 | 必填 | 说明 |
|------|------|------|
| `name` / `key` / `version` | 是 | `key` 为 `[a-zA-Z0-9_-]+`，不可为 `plugin` |
| `description` / `author` / `logo` / `readme` | 否 | 元信息 |
| `hook` / `singleton` / `config` | 否 | 后端钩子与配置 |
| `routes` | 否 | 服务端路由 + `handlers` |
| `menus` | 否 | 前端菜单；`appKey` 通常等于 `key` |

## 产物

- `bun run pack` → `release/{key}.vome`（先混淆后端，再 `vite build`）
- 安装只认 `.vome`：`POST /admin/base/module/install`
- 包内可含：`module.json` + `server/` + `web/` + `assets/`

## 后端

- `server/index.js`（CJS）：`extends BasePlugin`，`export const Plugin`
- `import { BasePlugin } from 'vome-plugin-runtime'`（打包 `--external`）

## 前端

- 源码：`web-src/`（Vue3 + Vite + TypeScript；`src/` 留给服务端）
- 构建产物：`web/`（`base: './'`，适配 `/vome/apps/{key}/`）
- 菜单 `appKey` = `key`；宿主 wujie 打开 `/vome/apps/{key}/`

## 禁止

- 依赖宿主 `.env`
- `key` 为 `plugin`
- 把 `node_modules` 打进包（后端用 bun bundle）
