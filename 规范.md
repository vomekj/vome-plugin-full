# Vome 插件规范（全栈 · vome-plugin-full）

> 仓库目录：`plugins/vome-plugin-full` · 示例 key：`scaffold-full`  
> 完整用法：`.vscode/AGENTS.md` → `.vscode/skills/SKILL.md`

## 清单 `module.json`

| 字段 | 必填 | 说明 |
|------|------|------|
| `name` / `key` / `version` | 是 | `key` 不可为 `plugin` |
| `hook` | 钩子场景建议 | 有 hook 必须有可加载 `Plugin` |
| `singleton` / `config.@local`/`@prod` | 否 | 禁止 `.env` |
| `routes` | 否 | 与 `handlers` 一一对应 |
| `menus` | 有 web 时建议 | `appKey` = `key` |

## 目录

| 路径 | 用途 |
|------|------|
| `src/` | **后端** → `server/index.js` |
| `web-src/` | **前端** → `web/`（`base: './'`） |
| `server/` / `web/` | 产物，勿手改业务 |

## 后端

- `extends BasePlugin` + `export const Plugin`
- handlers：`(ctx) => data`，网关 `/admin/ext/{key}/…`
- 公开方法名进 `plugins/scripts/obfuscator.options.ts` → `reservedNames`

## 前端

- 只改 `web-src/`；调本插件用 `/admin/ext/{key}/…`，调宿主用 `/admin/…`
- wujie + `appKey`；优先 hash 路由

## 产物

- `bun run pack`：混淆后端 + `build:web` → `release/{key}.vome`
- 包内：`module.json` + README + `server/` + `web/` + `assets/`
- 不要：`src/`、`web-src/`、`node_modules/`

## 禁止

- 混用 `src/` 与 `web-src/` 职责
- 依赖宿主 `.env`；`key` 为 `plugin`
- 漏 `build:web` 就 pack
