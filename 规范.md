# Vome 插件规范（全栈 · vome-plugin-full）

> 目录：`plugins/vome-plugin-full` · key：`scaffold-full`

## 可用 / 不可用

- **可用**：后端 invoke/ext + 前端 hostRequest/extPath + menus/wujie  
- **不可用**：插件内完整宿主 CRUD/IoC  

## 目录

| 路径 | 用途 |
|------|------|
| `src/` | 后端 → `server/` |
| `web-src/` | 前端 → `web/`（含 `lib/host-api.ts`） |
| `locales/` | 源语言包（包根，勿只放 `web-src/`） |

## 产物

`pack` = 混淆后端 + `build:web` + `locales/`；`PLUGIN_KEY` 与 `module.json.key` 一致。

## 多语言

- **源包**：包根 `locales/zh-CN.json`（与 `web-src` 构建分离；只放在 `web-src` 会导致安装后宿主扫不到）
- **译包权威**：宿主 `i18n_pack`；插件勿另起本地译包
- **运行时**：`web-src/lib/locale.ts` → `usePluginLocale()`；拉 `lang/enabled` + `pack/active`
- **UI**：右上角国旗切换；文案走 `t()`
